$import: numbers

Currency:
  USD: [ '\$', 'USD', 'US\$' ]
  EUR: [ '€', 'EUR' ]
  GBP: [ '£', 'GBP' ]

Money:
  - '(?<currency>${Currency})\s*(?<amount>${Number})'
  - '(?<amount>${Number})\s*(?<currency>${Currency})'