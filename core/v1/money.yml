$import: numbers

Currency:
  USD: [ '\$', 'USD', 'US\$' ]
  EUR: [ '€', 'EUR' ]
  GBP: [ '£', 'GBP' ]

Money:
  $fields:
    currency: Currency
    amount: Number
  $match:
    - '#{currency}\s*#{amount}'
    - '#{amount}\s*#{currency}'