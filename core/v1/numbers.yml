Scale:
  Thousand: 'thousand'
  Million: 'million'
  Billion: 'billion'

Number:
  Decimal: '\d+(,\d{3})*\.\d+'
  Integer:
    $fields:
      value: Number
      scale: Scale
    $match:
      - '\d+(,\d{3})*'
      - '#{value} #{scale}'

Percent:
  $fields: { value: Number }
  $match: '#{value}%'
